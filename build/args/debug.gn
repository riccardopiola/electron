# Component build configuration based on testing.gn
import("all.gn")
import("testing.gn")
is_debug = true # This is actually the only way component build works on windows
is_component_build = true

blink_symbol_level = 0
enable_nacl = false
